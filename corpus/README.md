# Corpus

No previous sample is trusted automatically. Audio may be admitted only after
its transcript, source, rights, metadata, evaluation annotations, and SHA-256
digest have been reviewed.

Self-recorded audio should document the recording setup. Web audio must include
its stable source URL, retrieval date, and redistribution status. A benchmark run
always uses the frozen local file and its digest, never the live URL.
